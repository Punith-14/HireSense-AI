[pytest]
DJANGO_SETTINGS_MODULE = backend.settings
pythonpath = backend
testpaths = backend/tests
addopts = -q
markers =
    hardware: tests requiring local webcam or microphone hardware
    performance: latency and throughput checks
